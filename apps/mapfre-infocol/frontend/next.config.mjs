/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: '/app/mapfre',
  allowedDevOrigins: ["localhost", "127.0.0.1", "192.168.1.34", "Mac-Pro-de-manu.local"],
};

export default nextConfig;
